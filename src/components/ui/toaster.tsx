"use client"

import { Toaster as Sonner } from "sonner"

/**
 * Renders the Sonner toast container for displaying global notifications.
 *
 * @returns The Sonner toast React element used to show toast notifications.
 */
export function Toaster() {
  return <Sonner />
}